import React from 'react';
import { Cards } from './MemberStyled';
import { Form, Row, Col, Select, Radio, Button, Upload } from 'antd';

import { Card } from 'antd';
const MemberInfo: React.FC = () => {
  return <Cards></Cards>;
};

export default MemberInfo;
